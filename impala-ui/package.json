{
  "name": "impala-ui",
  "version": "0.0.1",
  "private": true,
  "description": "Impala admin dashboard — static files served by Nginx (no build step). Vitest covers the pure JS modules.",
  "scripts": {
    "test": "vitest run"
  },
  "//": "vitest pinned: ^4 requires Node >=20 (rolldown uses util.styleText); dev environment runs Node 18 (2026-06-09)",
  "devDependencies": {
    "vitest": "^4"
  }
}
